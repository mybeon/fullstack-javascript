<%- include("inc/header") %>

<div class="container py-md-5 container--narrow">
  <%- include("inc/flash") %> <%- include("inc/sharedProfile") %>

  <div class="list-group">
    <% posts.forEach((post) => { %>
    <a href="/post/<%= post._id %>" class="list-group-item list-group-item-action">
      <img class="avatar-tiny" src="<%= post.author.avatar %>" />
      <strong><%= post.title %></strong> on <%= post.createdAt.getMonth() + 1%>/<%= post.createdAt.getDate() %>/<%= post.createdAt.getFullYear() %>
    </a>
    <% }) %>
  </div>
</div>

<%- include("inc/footer") %>
